services:
  - type: web
    name: food-delivery-app
    env: java-11  # Use Java 11 runtime
    plan: free
    startCommand: java -jar target/FoodDeliveryApplication.war  # Replace with the actual WAR file name
